# API Notes

## Base URL

- Local: `http://localhost:3001/api/v1`

## Current Endpoints

- `GET /health` -> `{ "ok": true }`

## TODO

- Add auth middleware (OIDC JWT verification)
- Add ticket/customer/project resource endpoints
- Add versioning and error contract documentation
